# 🍽 饮食管理助手 - 产品规格说明书

## 1. 产品概述

- **产品名称**：饮食管理助手（DietMate）
- **核心功能**：根据用户个人信息定制每日饮食摄入方案，支持每餐拍照打卡并自动分析卡路里，统计每日摄入
- **目标用户**：有体重管理、健康饮食需求的用户
- **核心价值**：一个人从零完成 App 设计、开发、部署

---

## 2. 功能模块

### 2.1 用户档案

| 字段 | 说明 |
|------|------|
| 昵称 | 用户昵称 |
| 性别 | 男/女 |
| 年龄 | 岁数 |
| 身高 | cm |
| 体重 | kg |
| 目标 | 减脂 / 维持 / 增肌 |
| 活动级别 | 久坐 / 轻度 / 中度 / 重度 |

**自动计算**：
- BMI = 体重(kg) / 身高(m)²
- 基础代谢率（BMR）= 10×体重 + 6.25×身高 - 5×年龄 + 性别系数（男+5，女-161）
- 每日所需热量 = BMR × 活动系数
- 三大营养素比例：蛋白质 30% / 碳水 40% / 脂肪 30%

### 2.2 每日饮食方案

- 根据上表自动生成每日推荐摄入（卡路里 + 三大营养素）
- 显示每餐建议摄入（早餐 30% / 午餐 40% / 晚餐 30%）
- 剩余配额实时显示

### 2.3 拍照打卡

- 支持拍照或从相册选择
- 每餐类型：早餐 / 午餐 / 晚餐 / 加餐
- 拍照后自动分析卡路里（通过图像识别 API）
- 显示：食物名称、估计卡路里、营养素估算
- 可手动修正分析结果

### 2.4 卡路里统计

- 今日已摄入 vs 目标
- 每餐详细记录列表
- 7日趋势图（柱状图）
- 超标/达标提示

### 2.5 打卡日历

- 月历视图标记每日打卡情况
- 点击日期查看当天饮食记录

---

## 3. 技术方案

### 3.1 前端
- **技术栈**：HTML5 + CSS3 + Vanilla JS（无框架，单文件可跑）
- **摄像头**：`<input type="file" capture="environment">` 调起相机
- **图表**：Canvas 手绘简易柱状图（无依赖库）
- **本地存储**：localStorage 保存用户档案和打卡记录

### 3.2 后端（可选，轻量 API）
- **技术栈**：Python Flask
- **端口**：5000
- **接口**：
  - `POST /api/analyze` — 接收图片 base64，返回卡路里分析结果
  - `GET /api/foods` — 查询已知食物数据库

### 3.3 卡路里分析方案
- **方案A**：调用外部食物识别 API（如 Google Cloud Vision + Nutrition API）
- **方案B（当前实现）**：内置常见食物数据库（100+种），按食物关键词匹配估算
- 拍照 → 提取文件名/描述 → 匹配食物库 → 返回估算值

### 3.4 数据存储
- **本地模式**：localStorage 全存储，离线可用
- **可选服务器**：Flask API + JSON 文件持久化

---

## 4. UI 设计

### 4.1 页面结构（5个Tab）

```
[首页] [打卡] [日历] [统计] [我的]
```

### 4.2 配色方案
- 主色：#4CAF50（绿色，健康感）
- 强调色：#FF9800（橙色，摄入提示）
- 背景：#FAFAFA
- 卡片：#FFFFFF
- 文字：#212121 / #757575

### 4.3 关键界面

**首页（今日概览）**
- 今日目标卡路里（大字）
- 圆形进度条（已摄入/目标）
- 剩余配额
- 快捷打卡按钮

**打卡页**
- 餐类选择（早餐/午餐/晚餐/加餐）
- 相机/相册按钮
- 分析结果展示卡
- 保存按钮

**统计页**
- 7日柱状图
- 总摄入 vs 目标
- 超标高亮

---

## 5. 交付物

```
diet-app/
├── index.html          ← 主页面（单文件，包含所有CSS/JS）
├── SPEC.md            ← 本规格说明书
├── README.md          ← 使用说明
├── server.py          ← Flask后端（可选）
└── foods.json         ← 食物数据库
```

---

## 6. 成功标准

- [ ] 用户完成个人信息录入，自动计算 BMI 和每日目标
- [ ] 拍照打卡后可看到卡路里分析结果
- [ ] 每日打卡记录正确统计
- [ ] 7日趋势图正常显示
- [ ] 打卡日历正确显示打卡状态
- [ ] 移动端适配良好
